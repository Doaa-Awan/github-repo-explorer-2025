{
  "version": 2,
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/index.js"
    }
  ],
    "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.ts" }
  ]
}
